const Homepage = () => {
    return (
        <section className="content">
            <h2 className="section-header">Select a section from sidebar.</h2>
        </section>
    );
};

export default Homepage;
